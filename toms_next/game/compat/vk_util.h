// compat/vk_util.h -- shadows src/engine/vk_util.h. game.cpp includes it on desktop builds but
// uses nothing from it; the bgfx build has no Vulkan code, so this is intentionally empty.
#pragma once
